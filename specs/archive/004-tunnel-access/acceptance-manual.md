# 004-tunnel-access · 手工验收记录（2026-09-10 回填）

> 导航：[spec.md](./spec.md) · [plan.md](./plan.md) · [tasks.md](./tasks.md) · 返回 [MOC](../../MOC.md)
>
> 需求方真机验收通过（2026-09-10"验收成功"），本文件为验收证据回填与遗留观察项登记。
> 自动化基线：`cargo test` **152 passed + 1 ignored**（2026-09-10 复跑）；`npm run build` 零错误。

## 1. 已验证项（真机实证）

| AC | 场景 | 证据 |
|----|------|------|
| AC1 | 切穿透 → 隧道在线 → 域名可访问 | frpc 日志"HTTPS 隧道启动成功, 绑定到域名 ai.jackqi.cn"；需求方手机（移动流量）访问通过（2026-09-10） |
| AC3 | 杀 frpc → 守护拉起 | taskkill 后守护 5s 内拉起并登录成功（2026-09-10 09:42 实测，含 Defender 删除文件后的重新拉起场景） |
| AC4 | 状态/错误展示 | 看板六态与错误摘要渲染正常；序列化缺陷（[object Object]）已修复复验 |
| AC5/AC6 | 双向切换 | 直连⇄穿透多次切换真机通过；ddns-go/frpc 启停与看板一致 |
| AC12/AC13 | DNS 指引与自动切换 | 切换自动暂停/激活解析（腾讯云后台核实记录状态）；检测条对齐后自动隐藏 |
| AC14/AC15 | 穿透设置区 | 隧道 ID/节点域名保存生效；校验（ID 纯数字）触发正常 |
| AC16 | 密钥脚本 | `set-frp-key.ps1` 拉起、不回显输入、直写 `.env`（末 4 位复核） |
| 端到端 | 穿透可达性 | 本机经隧道 `https://ai.jackqi.cn/` HTTP 200（1.5~2s，实连 103.167.151.254）；外部独立网络由需求方手机确认 |

## 2. 遗留观察项（实现已交付，未构造场景复验）

| AC | 场景 | 现状与触发条件 |
|----|------|----------------|
| AC2 | 换网（WiFi ⇄ 热点）自动重连 | frpc 自带重连 + 守护退避拉起 + 心跳红点反馈；待下次真实换网观察，异常按 bug 修复 |
| AC9 | 重启后的自启链按通道拉起 | 通道感知代码已交付（启动链跳过 ddns-go 的修复源自 2026-09-10 混挂实测回修）；待下次重启观察 ddns-go 不再被拉起 |
| AC10 | 收摊退出停 frpc | 退出钩子（ExitRequested → TunnelManager.stop）已交付；**2026-09-09 曾实测残留**（修复前），修复后待复验；兜底：NSIS preInstall 钩子会在覆盖安装前强制停 frpc |

## 3. 验收期修复记录（详见 spec 变更记录）

1. DNS 探测 Type 字符串比较失效 → 字段存在性过滤（c3b0d22）
2. TC3 签名三头形态被网关拒 → 对齐官方 SDK 两头 + 空行形态（3edf23d）
3. 守护死锁（自愈持锁调 restart）→ IO 移出锁外（ea8fbb7）
4. 隧道"在线"误导（进程在 ≠ 登录成功）→ 日志实况判定（ea8fbb7）
5. HealthKind 序列化 [object Object] → 纯字符串（680151a）
6. Defender 误报删 frpc → 需求方加排除 + 文件恢复（10:49 检测记录）
