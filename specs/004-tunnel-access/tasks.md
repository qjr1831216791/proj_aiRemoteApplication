# 004-tunnel-access · 任务清单（tasks）

> 导航：[spec.md](./spec.md) · [plan.md](./plan.md) · 返回 [MOC](../MOC.md)

- **状态**: 进行中 <!-- 未开始 | 进行中 | 已完成 -->
- **最后更新**: 2026-09-10（收口 done）

> 拆解原则：每个任务可在一天内完成、有明确完成标志、可追溯到验收标准（AC）。
> 任务状态标记：`[ ]` 待办 · `[~]` 进行中 · `[x]` 完成

## 阶段 1: 准备与依赖

- [x] T1 frpc 部署：定制版 `0.51.0-sakura-14`（SHA256 `b70526…5234`）**入库随包分发**（`resources/bin/frpc.exe` → tauri bundle.resources，需求方决策"开箱自带"），运行时资源目录优先/栈目录回退；`frpc -v` 验证通过（依赖: 无）（验收: 二进制可执行、版本合规、进包）
- [x] T2 `.env` 约定落地：根 `.env.example` 登记 `SAKURA_FRP_KEY`；栈目录 `.env` 指引文案定稿（依赖: 无）（验收: AC7 配置引导文案素材就绪）
- [x] T3 SakuraFrp 建隧道操作指引文案定稿（海外建站节点 → HTTPS 类型 + 本地 `127.0.0.1:443` + 绑定 `ai.jackqi.cn` + HTTP 重定向开关），需求方按指引完成建隧道与 `.env` 写入（依赖: 无）（验收: AC1 前置——需求方持有隧道 ID 与节点域名）

## 阶段 2: Rust 后端核心

- [x] T4 `settings.rs` 扩展：`AccessChannel` / `TunnelConfig` / `tunnel_enabled` 字段 + 向后兼容迁移（缺省 Direct）+ 单测（迁移往返、非法值回落）（依赖: 无）（验收: AC8 持久化基础）
- [x] T5 新增 `tunnel.rs`：frpc spawn/停止（`.env` 读 key 拼装 `-f key:id`）、进程存活探测、key 不入日志断言（依赖: T1/T4）（验收: AC1 启动、AC3 存活探测）
- [x] T6 `orchestrator.rs` 集成：`ComponentId::Frpc` 组件化；守护决策纯函数（应运行∧死亡∧退避时钟 → 重启，5s/15s/60s 封顶）+ 接入轮询；通道切换原子操作（停旧启新 + 持久化）+ 状态机单测（依赖: T5）（验收: AC3/AC5/AC6/AC8）
- [x] T7 DNS 对齐检测：`nslookup` 查询封装 + 判定纯函数（CNAME→节点域名 / 残留 CNAME / A 恢复 / 无记录 四态）+ 输出样本单测；后增补 `dns_api.rs`（TC3 签名对齐官方 SDK + 暂停/激活调和，真机验证 HTTP 200）（依赖: T4）（验收: AC12/AC13 + 自动切换）
- [x] T8 `commands.rs` 接线：`switch_channel` / `set_tunnel_enabled` / `check_dns_alignment` + 事件扩展；现有测试回归全绿（依赖: T6/T7）（验收: AC5/6/7/11 命令层）

## 阶段 3: 前端

- [x] T9 「访问通道」卡与穿透设置区：当前通道显示、切换确认流程（含风险/步骤说明、防重复点击、失败提示）、穿透配置引导态、设置区（隧道 ID/节点域名编辑保存 + 校验，密钥仅 `.env` 指引）（依赖: T8）（验收: AC5/6/7/11/14/15）
- [x] T10 隧道状态显示与 DNS 指引条：四态渲染、错误摘要、双向指引显隐（依赖: T8/T9）（验收: AC1/4/12/13）
- [x] T11 双语文案全覆盖（中/英，沿 AC25 语言机制）（依赖: T9/T10）（验收: 002 同款语言切换检查）

## 阶段 4: 测试与收尾

- [x] T12 自动化测试收口：`cargo test` 全绿（含既有 134 项回归）；判定函数覆盖率对齐 plan §6（依赖: T4-T8）
- [x] T13 真机验收：外部独立网络探测 `https://ai.jackqi.cn/`、热点⇄WiFi 切换实测、杀 frpc 守护拉起实测、双通道切换回退实测；对照 spec 逐条勾选 AC（依赖: T12 + T3 需求方建隧道完成）
- [x] T14 文档收口：`CHANGELOG.md` Unreleased 登记、`specs/MOC.md` 状态流转、手工验收清单回填（沿 001 acceptance-manual 先例）（依赖: T13）

## 完成标志（DoD 检查）

- [x] spec.md AC：13/16 真机验证通过并勾选；AC2（换网重连）/AC9（重启自启链）/AC10（退出清理复验）实现已交付、待日常使用自然复验（spec 变更记录末行如实注明，异常按 bug 修复处理）
- [x] 自动化测试全部通过（152 项 + 1 ignored，2026-09-10 复跑）
- [x] 相关文档已更新
- [x] 本文件全部任务勾选完毕
