{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力（最小集，随任务推进逐项放宽）",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-show"]
}
